{ pkgs, ... }: {
  programs.lf = {
      enable = true;
      # cmdKeybindings = {};
      # commands = {};
      # extraConfig = {};
      # keybindings = {};
      # settings = {};
      # previewer = {
      #   source = ;
      #   keybinding = "p";
      # };
    };
}
